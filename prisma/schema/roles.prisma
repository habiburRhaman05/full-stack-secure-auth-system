enum UserRole {
    customer
    admin
    moderator
    support
}

enum UserStatus {
    active
    banned
    deleted
}

enum AuthProvider {
    local
    google
}

enum DeviceType {
    desktop
    mobile
    tablet
    unknown
}

enum RevokeReason {
    logout
    logout_all
    password_change
    password_reset
    admin_revoke
    token_theft
    expired
    rotated
}

enum VerificationType {
    email_verify
    two_factor
    password_reset
}
