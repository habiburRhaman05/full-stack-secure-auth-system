enum UserRole {
    customer
    admin
    moderator
    support
}

enum UserStatus {
    active
    banned
    deleted
}

enum AuthType {
    local
    oauth
    both
}

enum OAuthProvider {
    google
    github
    facebook
}

enum DeviceType {
    desktop
    mobile
    tablet
}

enum RevokeReason {
    logout
    logout_all
    password_change
    admin_revoke
    token_theft
    expired
}

enum LoyaltyTier {
    bronze
    silver
    gold
    platinum
}

enum AuditAction {
    REGISTER
    EMAIL_VERIFIED
    LOGIN
    LOGIN_FAILED
    LOGOUT
    LOGOUT_ALL
    TOKEN_REFRESH
    TOKEN_THEFT
    PASSWORD_CHANGE
    PASSWORD_RESET_REQUEST
    PASSWORD_RESET_COMPLETE
    PROFILE_UPDATE
    ROLE_CHANGE
    ACCOUNT_DELETED
    USER_BANNED
    USER_UNBANNED
    GOOGLE_LINKED
    TWO_FA_ENABLED
    TWO_FA_DISABLED
    IMPERSONATION
    UNAUTHORIZED_ACCESS
}

enum AuditStatus {
    success
    failed
}
